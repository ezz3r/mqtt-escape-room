[project]
name = "mqtt-escape-room"
version = "0.1.0"
description = "An escape room game using MQTT for communication."

requires-python = ">=3.13,<3.14"

dependencies = [
  "dotenv>=0.9.9",
  "paho-mqtt",
  "pygame>=2.6.1",
  "python-dotenv>=1.2.3",
]
